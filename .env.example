# WP Launcher Environment Configuration
# Copy this to .env and fill in your values, or run: bash install.sh

# ─── Mode ────────────────────────────────────────────────────────────────────
# "agency" = full auth, limits, restrictions for demo hosting (default)
# "local"  = no auth, no limits, no WP restrictions — local dev environment
APP_MODE=agency

# ─── Core ────────────────────────────────────────────────────────────────────
NODE_ENV=production

# Base domain (subdomains will be *.BASE_DOMAIN)
BASE_DOMAIN=demo.example.com

# Public URL (used in verification emails and links)
PUBLIC_URL=https://demo.example.com

# ─── Secrets (generate strong random values!) ────────────────────────────────
# API key for admin access
API_KEY=change-me-to-a-secure-random-string

# JWT secret for user authentication tokens
JWT_SECRET=change-me-to-a-secure-random-string

# Internal key for API <-> Provisioner communication
PROVISIONER_INTERNAL_KEY=change-me-to-a-secure-random-string

# JWT token lifetime
JWT_EXPIRES_IN=7d

# ─── CORS ────────────────────────────────────────────────────────────────────
# Allowed origins (comma-separated). Leave empty to use BASE_DOMAIN default.
CORS_ALLOWED_ORIGINS=https://demo.example.com

# ─── Email ────────────────────────────────────────────────────────────────────
# Provider: "smtp" (default) or "brevo" (HTTP API — bypasses SMTP port blocks)
# Many VPS providers (DigitalOcean, etc.) block outbound SMTP ports (25/465/587).
# If SMTP times out, switch to EMAIL_PROVIDER=brevo and set BREVO_API_KEY.
EMAIL_PROVIDER=smtp

# SMTP settings (used when EMAIL_PROVIDER=smtp)
# Set to "mailpit" / port 1025 for local dev (uses built-in Mailpit)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=WP Launcher <noreply@demo.example.com>

# Brevo HTTP API key (used when EMAIL_PROVIDER=brevo)
# Get your API key from: Brevo Dashboard > Settings > SMTP & API > API Keys
# This bypasses SMTP entirely — uses HTTPS (port 443) which is never blocked.
BREVO_API_KEY=

# ─── WordPress ───────────────────────────────────────────────────────────────
# Docker image for demo sites (built by scripts/build-wp-image.sh)
WP_IMAGE=wp-launcher/wordpress:latest

# Max active sites per user (0 = unlimited, default: 3)
MAX_SITES_PER_USER=3

# Maximum total active demo sites across all products (0 = unlimited, default: 50)
MAX_TOTAL_SITES=50

# Per-container resource limits
CONTAINER_MEMORY=268435456
CONTAINER_CPU=0.5

# Host path to product-assets directory (for bind-mounting local plugins/themes into containers)
# Set this to the absolute path of the product-assets folder on the host machine
PRODUCT_ASSETS_PATH=/opt/wp-launcher/product-assets

# ─── Let's Encrypt SSL ──────────────────────────────────────────────────────
# Email for Let's Encrypt certificate notifications
ACME_EMAIL=your@email.com

# ─── Cloudflare (optional — for wildcard SSL via DNS challenge) ──────────────
# Uncomment and fill in if using Cloudflare DNS challenge (recommended)
# CF_API_EMAIL=your@email.com
# CF_DNS_API_TOKEN=your-cloudflare-api-token
